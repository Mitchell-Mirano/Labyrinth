#ifndef PLAYER_H
#define PLAYER_H

class Player
    {
        private:
            /* data */
        public:
            Player();
            
            void ImputPlayer();
            void ResetPosition();
            int x,y;
            int beforex, beforey;

        protected:

    };

#endif //PLAYER_H